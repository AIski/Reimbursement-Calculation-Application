import { ReimbursementClaim } from './reimbursement-claim';

describe('ReimbursementClaim', () => {
  it('should create an instance', () => {
    expect(new ReimbursementClaim()).toBeTruthy();
  });
});
