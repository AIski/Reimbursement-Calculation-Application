import { LimitsConfiguration } from './limits-configuration';

describe('LimitsConfiguration', () => {
  it('should create an instance', () => {
    expect(new LimitsConfiguration()).toBeTruthy();
  });
});
